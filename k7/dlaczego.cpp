#include <bits/stdc++.h> 
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    cout << round(0.0501099 * 100.0) / 100.0;
    cout << round(0.059 * 100.0) / 100.0;

    return 0;
}